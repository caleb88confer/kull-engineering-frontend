import {useEffect} from 'react';

function MasteringLocation ({setLocation}) {
    useEffect(() => {
        setLocation(8);
        window.scrollTo(0, 0)
    })
    return (
        <>
        </>
    );
}
export default MasteringLocation;